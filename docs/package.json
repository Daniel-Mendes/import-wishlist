{
  "scripts": {
    "dev": "parcel serve index.html",
    "build": "netlify-lambda build netlify/functions && parcel build index.html",
    "postinstall": "netlify-lambda install"
  },
  "dependencies": {
    "igdb-api-node": "^5.0.2"
  },
  "devDependencies": {
    "netlify-lambda": "^2.0.15",
    "parcel": "^2.8.3"
  }
}
